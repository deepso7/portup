{
  "name": "@portup/engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --outfile dist/portup src/main.ts",
    "check": "tsc --noEmit",
    "test": "bun run build && bun test"
  },
  "dependencies": {
    "effect": "3.22.1"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "bun": "1.4.0",
    "typescript": "catalog:"
  }
}
