{
  "name": "@portup/engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --outfile dist/portup src/main.ts",
    "check": "tsc --noEmit",
    "test": "bun run build && bun test"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-rc.111",
    "@effect/sql-sqlite-bun": "4.0.0-rc.111",
    "drizzle-orm": "1.0.0-rc.5-169397b",
    "effect": "4.0.0-rc.111"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "bun": "1.4.0",
    "typescript": "catalog:"
  }
}
